<nav>
  <ul>
    <% categories.forEach(item => { %>
      <li>
        <a href="<%= item.toLowerCase() === 'everything' ? '/' : '/' + item.toLowerCase() %>">
          <%= item %>
        </a>
    </li>
    <% }) %>
  </ul>
</nav>
